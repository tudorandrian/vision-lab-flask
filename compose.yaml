# A fixed project name, so the container, volume and network names start with
# vision-lab-flask whatever the folder the repository was cloned into.
name: vision-lab-flask

services:
  app:
    build: .
    ports:
      - "127.0.0.1:8000:8000"   # loopback only: the app has no authentication
    volumes:
      - vision-lab-data:/data
    environment:
      VISION_LAB_JOB_TTL_MINUTES: "60"
    init: true   # tini becomes PID 1 and forwards SIGTERM to the app process
    mem_limit: 2g
    cap_drop: [ALL]
    security_opt: ["no-new-privileges:true"]
    restart: unless-stopped

volumes:
  vision-lab-data:
